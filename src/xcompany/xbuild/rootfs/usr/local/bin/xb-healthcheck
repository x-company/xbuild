#!/usr/bin/execlineb -P


background {

    if { s6-test -d /etc/xbuild/health.d }
}

if [ -d /etc/xinit/health.d ]
then
    for file in /etc/xinit/health.d/*
    do
        [ -e "$file" ] || continue

        log "Execute Health Check '$file'"
        bash "$file"
    done
fi
