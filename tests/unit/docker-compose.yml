version: "3.7"

services:
  xbuild:
    image: xcompany/xbuild:unit
    build:
      context: ../../
      target: dev
    volumes:
      # Map the current Source Folder
      - ../..:/workspace
    command: "/usr/bin/bats /workspace/tests/unit/"
